table! {
    users_data (id) {
        id -> Varchar,
        user_name -> Varchar,
        email -> Varchar,
        password -> Varchar,
        is_superuser -> Bool,
        is_active -> Bool,
    }
}
